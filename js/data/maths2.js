// Corrigé — Mathématiques 2 — BANQUE PT 2025
// Chargé dynamiquement par epreuve.html?id=maths2
(function () {
  const ep = CATALOGUE.find(e => e.id === 'maths2');
  if (!ep) return;

  // ep.mu    = ;
  // ep.sigma = ;

  ep.parties = [];
}());
