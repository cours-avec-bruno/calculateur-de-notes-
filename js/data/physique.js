// Corrigé — Physique-Chimie — BANQUE PT 2025
// Chargé dynamiquement par epreuve.html?id=physique
(function () {
  const ep = CATALOGUE.find(e => e.id === 'physique');
  if (!ep) return;

  // ep.mu    = ;
  // ep.sigma = ;

  ep.parties = [];
}());
