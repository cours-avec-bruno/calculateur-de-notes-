// Corrigé — Sciences de l'Ingénieur — BANQUE PT 2025
// Chargé dynamiquement par epreuve.html?id=si
(function () {
  const ep = CATALOGUE.find(e => e.id === 'si');
  if (!ep) return;

  // ep.mu    = ;
  // ep.sigma = ;

  ep.parties = [];
}());
