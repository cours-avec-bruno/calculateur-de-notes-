// Corrigé — Français-Philosophie — BANQUE PT 2025
// Chargé dynamiquement par epreuve.html?id=francais
(function () {
  const ep = CATALOGUE.find(e => e.id === 'francais');
  if (!ep) return;

  // ep.mu    = ;
  // ep.sigma = ;

  ep.parties = [];
}());
