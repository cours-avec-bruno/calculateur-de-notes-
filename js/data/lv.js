// Corrigé — Langue Vivante — BANQUE PT 2025
// Chargé dynamiquement par epreuve.html?id=lv
(function () {
  const ep = CATALOGUE.find(e => e.id === 'lv');
  if (!ep) return;

  // ep.mu    = ;
  // ep.sigma = ;

  ep.parties = [];
}());
