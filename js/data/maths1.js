// Corrigé — Mathématiques 1 — BANQUE PT 2025
// Chargé dynamiquement par epreuve.html?id=maths1
(function () {
  const ep = CATALOGUE.find(e => e.id === 'maths1');
  if (!ep) return;

  // ep.mu    = ;   // à renseigner
  // ep.sigma = ;   // à renseigner

  ep.parties = [
    // À remplir
  ];
}());
